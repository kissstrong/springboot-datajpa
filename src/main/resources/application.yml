spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      #更新或创建数据表结构
      ddl-auto: update
      #在控制台显示sql语句
    show-sql: true


